// import { Server, Request, Response } from "miragejs";

// export default function transactionFakeApi(server: Server) { 

//     server.get("/transactions", (schema) => {
//         return {
            
//           data: schema.transactions.all().models, // Return accounts, not just transactions
//         };
//       });
// }